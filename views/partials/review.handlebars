<div>
	<h4>
		<a href="/movies/{{this.movieId}}/reviews/{{this._id}}">
			TITLE: {{this.title}}
		</a>
	</h4>

	<h4>
		MOVIE: {{this.movieTitle}}
	</h4>

	<h4>
		REVIEW: {{this.description}}
	</h4>

	<p>
		<a href="/reviews/{{this._id}}/edit">
		Edit
		</a>
	</p>

		<form method="POST" action="/movies/{{this.movieId}}/reviews/{{this._id}}?_method=DELETE">
			<button class="btn btn-primary" type="submit">
				Delete
			</button>
		</form>
	</p>
</div>